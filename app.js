if(!window.phoneLedger){const enc=new TextEncoder(),bytes=s=>enc.encode(s),join=parts=>{const size=parts.reduce((n,p)=>n+p.length,0),out=new Uint8Array(size);let at=0;for(const p of parts){out.set(p,at);at+=p.length}return out},u16=n=>{const b=new Uint8Array(2);new DataView(b.buffer).setUint16(0,n,true);return b},u32=n=>{const b=new Uint8Array(4);new DataView(b.buffer).setUint32(0,n>>>0,true);return b},crc=b=>{let c=~0;for(const x of b){c^=x;for(let k=0;k<8;k++)c=(c>>>1)^((c&1)?0xedb88320:0)}return(~c)>>>0},zip=async files=>{let offset=0,body=[],dir=[];for(const f of files){const name=bytes(f.name),raw=bytes(f.text),packed=new Uint8Array(await new Response(new Blob([raw]).stream().pipeThrough(new CompressionStream('deflate-raw'))).arrayBuffer()),check=crc(raw),head=join([u32(0x04034b50),u16(20),u16(0),u16(8),u16(0),u16(0),u32(check),u32(packed.length),u32(raw.length),u16(name.length),u16(0),name]);body.push(head,packed);dir.push(join([u32(0x02014b50),u16(20),u16(20),u16(0),u16(8),u16(0),u16(0),u32(check),u32(packed.length),u32(raw.length),u16(name.length),u16(0),u16(0),u16(0),u16(0),u32(0),u32(offset),name]));offset+=head.length+packed.length}const central=join(dir);return join([...body,central,u32(0x06054b50),u16(0),u16(0),u16(files.length),u16(files.length),u32(central.length),u32(offset),u16(0)])},xml=s=>String(s??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;'),col=n=>{let s='';for(;n>0;n=Math.floor((n-1)/26))s=String.fromCharCode(65+(n-1)%26)+s;return s},sheet=rows=>'<?xml version="1.0" encoding="UTF-8" standalone="yes"?><worksheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main"><sheetData>'+rows.map((row,i)=>'<row r="'+(i+1)+'">'+row.map((v,j)=>typeof v==='number'?'<c r="'+col(j+1)+(i+1)+'"><v>'+v+'</v></c>':'<c r="'+col(j+1)+(i+1)+'" t="inlineStr"><is><t>'+xml(v)+'</t></is></c>').join('')+'</row>').join('')+'</sheetData></worksheet>',makeXlsx=async sheets=>{const files=[{name:'[Content_Types].xml',text:'<?xml version="1.0" encoding="UTF-8"?><Types xmlns="http://schemas.openxmlformats.org/package/2006/content-types"><Default Extension="rels" ContentType="application/vnd.openxmlformats-package.relationships+xml"/><Default Extension="xml" ContentType="application/xml"/><Override PartName="/xl/workbook.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet.main+xml"/>'+sheets.map((_,i)=>'<Override PartName="/xl/worksheets/sheet'+(i+1)+'.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml"/>').join('')+'</Types>'},{name:'_rels/.rels',text:'<?xml version="1.0" encoding="UTF-8"?><Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships"><Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/officeDocument" Target="xl/workbook.xml"/></Relationships>'},{name:'xl/workbook.xml',text:'<?xml version="1.0" encoding="UTF-8"?><workbook xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main" xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships"><sheets>'+sheets.map((s,i)=>'<sheet name="'+xml(s.name)+'" sheetId="'+(i+1)+'" r:id="rId'+(i+1)+'"/>').join('')+'</sheets></workbook>'},{name:'xl/_rels/workbook.xml.rels',text:'<?xml version="1.0" encoding="UTF-8"?><Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships">'+sheets.map((_,i)=>'<Relationship Id="rId'+(i+1)+'" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/worksheet" Target="worksheets/sheet'+(i+1)+'.xml"/>').join('')+'</Relationships>'},...sheets.map((s,i)=>({name:'xl/worksheets/sheet'+(i+1)+'.xml',text:sheet(s.rows)}))],z=await zip(files);let bin='';for(let i=0;i<z.length;i+=8192)bin+=String.fromCharCode(...z.subarray(i,i+8192));return btoa(bin)};window.phoneLedger={load:async()=>JSON.parse(localStorage.getItem('phonePurchaseData')||'{"records":[],"buyers":[],"stores":[],"products":[],"colorStyles":[]}'),save:async data=>localStorage.setItem('phonePurchaseData',JSON.stringify(data)),xlsx:makeXlsx}}
const UPC_MODELS = {
  '195950637151':'iPhone 17 Pro Max · 银色 · 256GB',
  '195950626162':'iPhone 17 Pro · 银色 · 256GB',
  '195950638035':'iPhone 17 Pro Max · 橙色 · 512GB',
  '195950638028':'iPhone 17 Pro Max · 银色 · 512GB',
  '195950638011':'iPhone 17 Pro Max · 蓝色 · 256GB',
  '195950638004':'iPhone 17 Pro Max · 橙色 · 256GB',
  '195950685784':'iPhone 16 Pro Max · 沙漠金色 · 256GB',
  '195950385417':'iPhone 16 Pro · 原色钛金属 · 256GB'
};
const $ = id => document.getElementById(id);
const DEFAULT_BUYERS = [];
const DEFAULT_STORES = [];
let records = [], buyerChoices = [], storeChoices = [], products = [], colorStyles = [];
async function load(){
  try { return await window.phoneLedger.load(); }
  catch { return {records:[],buyers:[],stores:[]}; }
}
async function save(){
  $('saveState').textContent = '正在保存…';
  try { await window.phoneLedger.save({records,buyers:buyerChoices,stores:storeChoices,products,colorStyles}); $('saveState').textContent='已保存到本机'; }
  catch { $('saveState').textContent='保存失败，请检查软件文件夹权限'; }
}
function localNow(){const d=new Date();d.setMinutes(d.getMinutes()-d.getTimezoneOffset());return d.toISOString().slice(0,16)}
function money(n){return new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(n)}
function dayKey(value){const d=new Date(value);d.setMinutes(d.getMinutes()-d.getTimezoneOffset());return d.toISOString().slice(0,10)}
function csvCell(value){const s=String(value??'');return /[",\n]/.test(s)?'"'+s.replaceAll('"','""')+'"':s}
function downloadXlsx(name,base64){const raw=atob(base64),bytes=new Uint8Array(raw.length);for(let i=0;i<raw.length;i++)bytes[i]=raw.charCodeAt(i);const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([bytes],{type:'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'}));a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
function updateOptions(){
  const fill = (id, values) => $(id).replaceChildren(...[...new Set(values.map(v => String(v).trim()).filter(Boolean))].sort((a,b)=>a.localeCompare(b)).map(value => { const option=document.createElement('option'); option.value=value; return option; }));
  fill('buyerOptions', [...DEFAULT_BUYERS, ...buyerChoices]);
  fill('storeOptions', [...DEFAULT_STORES, ...storeChoices]);
}
function renderTodayStats(){const today=dayKey(new Date()),groups=new Map();records.filter(r=>dayKey(r.purchasedAt)===today).forEach(r=>{const g=groups.get(r.buyer)||{buyer:r.buyer,count:0,total:0,models:new Map()};g.count++;g.total+=Number(r.price);g.models.set(r.model,(g.models.get(r.model)||0)+1);groups.set(r.buyer,g)});const body=$('todayStats');body.innerHTML='';[...groups.values()].sort((a,b)=>a.buyer.localeCompare(b.buyer)).forEach(g=>{const tr=document.createElement('tr'),models=[...g.models.entries()].map(([name,count])=>`${name} ×${count}`).join('；');tr.innerHTML=`<td>${g.buyer}</td><td class="price">${g.count}</td><td class="price">${money(g.total)}</td><td>${models}</td>`;body.append(tr)});$('todayStatsEmpty').hidden=groups.size>0}
function renderModel(cell,model){cell.replaceChildren();const style=[...colorStyles].sort((a,b)=>b.name.length-a.name.length).find(s=>s.name&&model.includes(s.name));if(!style){cell.textContent=model;return}const [before,after]=model.split(style.name);cell.append(document.createTextNode(before));const tag=document.createElement('span');tag.className='color-tag';tag.textContent=style.name;tag.style.color=style.textColor;tag.style.backgroundColor=style.bgColor;tag.style.borderColor=style.textColor+'55';cell.append(tag,document.createTextNode(after))}
function render(){const q=$('search').value.trim().toLowerCase();const list=records.filter(r=>[r.imei,r.upc,r.buyer].some(v=>String(v??'').toLowerCase().includes(q))).sort((a,b)=>b.purchasedAt.localeCompare(a.purchasedAt));const body=$('records');body.innerHTML='';const t=$('rowTemplate');for(const r of list){const tr=t.content.firstElementChild.cloneNode(true);tr.dataset.id=r.id;tr.querySelector('.time').textContent=new Date(r.purchasedAt).toLocaleString('zh-CN',{year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hour12:false});tr.querySelector('.imei').textContent=r.imei||'—';renderModel(tr.querySelector('.phone'),r.model);tr.querySelector('.buyer').textContent=r.buyer;tr.querySelector('.price').textContent=money(r.price);tr.querySelector('.store').textContent=r.store;body.append(tr)}const total=records.reduce((s,r)=>s+Number(r.price),0);$('summary').textContent=`共 ${records.length} 条 · 总金额 ${money(total)}`;$('emptyState').hidden=Boolean(list.length);renderTodayStats();updateOptions()}
let scanAdvanceTimer;
function scheduleImeiFocus(){clearTimeout(scanAdvanceTimer);document.documentElement.dataset.upcScanDone='1';scanAdvanceTimer=setTimeout(()=>{delete document.documentElement.dataset.upcScanDone;$('imei').focus()},180)}
function resolveScannedUpc(){const field=$('model');if(field.readOnly)return;const upc=field.value.replace(/\D/g,'');field.value=upc;const p=products.find(x=>x.upc===upc);if(p){const name=[p.model,p.color,p.storage].filter(Boolean).join(' · ');$('upc').value=upc;field.value=name;field.readOnly=true;field.classList.add('matched-model');$('upcHint').textContent='已识别：'+name;$('upcHint').className='hint match';scheduleImeiFocus()}else if(upc){$('upc').value='';$('upcHint').textContent='此 UPC 未在管理页面添加。';$('upcHint').className='hint new'}else{$('upc').value='';$('upcHint').textContent='请在此框直接扫描 UPC。保存时自动记录电脑当前时间。';$('upcHint').className='hint'}}
document.addEventListener('keydown',e=>{if(document.documentElement.dataset.upcScanDone==='1'&&(e.key==='Tab'||e.key==='Enter')){e.preventDefault();e.stopImmediatePropagation()}},true);
$('model').addEventListener('input',resolveScannedUpc);$('search').addEventListener('input',render);
const isIPhone=/iPhone|iPod/.test(navigator.userAgent);
function syncAmountKeypadViewport(){const keypad=$('amountKeypad'),viewport=window.visualViewport,vars=['--keypad-gap','--keypad-pad','--keypad-header-height','--keypad-key-height','--keypad-number-size','--keypad-letter-size','--keypad-icon-size','--keypad-done-size','--keypad-radius'];if(!isIPhone||keypad.hidden||!viewport){['width','left','right','top','bottom',...vars].forEach(name=>keypad.style.removeProperty(name));return}const scale=Math.max(1,viewport.scale||1),width=viewport.width*scale,fit=(low,high,ratio)=>Math.max(low,Math.min(high,width*ratio))/scale;keypad.style.setProperty('--keypad-gap',`${fit(6,8,.02)}px`);keypad.style.setProperty('--keypad-pad',`${fit(7,9,.023)}px`);keypad.style.setProperty('--keypad-header-height',`${fit(26,30,.077)}px`);keypad.style.setProperty('--keypad-key-height',`${fit(42,52,.13)}px`);keypad.style.setProperty('--keypad-number-size',`${fit(18,21,.054)}px`);keypad.style.setProperty('--keypad-letter-size',`${fit(6.5,7.5,.019)}px`);keypad.style.setProperty('--keypad-icon-size',`${fit(19,23,.059)}px`);keypad.style.setProperty('--keypad-done-size',`${fit(12,14,.036)}px`);keypad.style.setProperty('--keypad-radius',`${fit(15,17,.043)}px`);const top=Math.max(viewport.offsetTop,viewport.offsetTop+viewport.height-keypad.offsetHeight);keypad.style.width=`${viewport.width}px`;keypad.style.left=`${viewport.offsetLeft}px`;keypad.style.right='auto';keypad.style.top=`${top}px`;keypad.style.bottom='auto'}
function hideAmountKeypad(){$('amountKeypad').hidden=true;syncAmountKeypadViewport()}
function showAmountKeypad(){const keypad=$('amountKeypad');keypad.hidden=false;requestAnimationFrame(()=>{syncAmountKeypadViewport();requestAnimationFrame(syncAmountKeypadViewport)})}
function configureAmountInput(){const price=$('price');document.body.classList.toggle('phone-keypad-enabled',isIPhone);price.readOnly=isIPhone;price.inputMode=isIPhone?'none':'decimal';if(!isIPhone)hideAmountKeypad();else syncAmountKeypadViewport()}
configureAmountInput();
window.addEventListener('resize',configureAmountInput);
window.visualViewport?.addEventListener('resize',syncAmountKeypadViewport);
window.visualViewport?.addEventListener('scroll',syncAmountKeypadViewport);
function cleanAmount(value){let out='',dot=false,decimals=0;for(const char of String(value)){if(char>='0'&&char<='9'){if(dot&&decimals>=2)continue;out+=char;if(dot)decimals++;continue}if((char==='.'||char===',')&&!dot){out+='.';dot=true}}return out}
$('price').addEventListener('input',event=>{if(!isIPhone)event.target.value=cleanAmount(event.target.value)});
$('price').addEventListener('click',()=>{if(isIPhone)showAmountKeypad()});
$('amountKeypad').addEventListener('click',event=>{const key=event.target.closest('[data-key]')?.dataset.key;if(!key)return;const price=$('price');if(key==='done'){hideAmountKeypad();return}if(key==='back'){price.value=price.value.slice(0,-1);return}price.value=cleanAmount(price.value+key)});
$('purchaseForm').addEventListener('reset',()=>setTimeout(()=>{clearTimeout(scanAdvanceTimer);delete document.documentElement.dataset.upcScanDone;const field=$('model');field.readOnly=false;field.classList.remove('matched-model');field.placeholder='直接用扫码枪扫描 UPC';$('upc').value='';hideAmountKeypad();$('upcHint').textContent='请在此框直接扫描 UPC。保存时自动记录电脑当前时间。';$('upcHint').className='hint';field.focus()},0));
$('purchaseForm').addEventListener('submit',async e=>{e.preventDefault();const upc=$('upc').value.trim(),imei=$('imei').value.trim(),buyer=$('buyer').value.trim(),store=$('store').value.trim(),price=Number($('price').value.trim().replace(',','.'));if(!products.some(p=>p.upc===upc)){alert('请先在管理页面添加这个手机 UPC。');return}if(!buyerChoices.includes(buyer)||!storeChoices.includes(store)){alert('请先在管理页面添加并选择购买人和购买门店。');return}if(!Number.isFinite(price)||price<0){alert('请输入正确的购买金额。');$('price').focus();return}const existing=imei&&records.find(r=>r.imei===imei);if(existing){alert(`此 IMEI 已有采购记录，无法重复保存。\n购买人：${existing.buyer}\n门店：${existing.store}\n时间：${new Date(existing.purchasedAt).toLocaleString('zh-CN',{hour12:false})}`);return}records.push({id:crypto.randomUUID(),upc,imei,model:$('model').value.trim(),buyer,price,store,purchasedAt:new Date().toISOString()});await save();e.target.reset();render()});
$('records').addEventListener('click',async e=>{if(!e.target.matches('.delete'))return;const id=e.target.closest('tr').dataset.id;records=records.filter(r=>r.id!==id);await save();render()});
$('clearRecords').addEventListener('click',async()=>{if(confirm('确定清空全部采购记录吗？此操作无法撤销。')){records=[];await save();render()}});
$('exportBtn').addEventListener('click',()=>{const blob=new Blob([JSON.stringify({app:'手机采购入账',exportedAt:new Date().toISOString(),records,buyers:buyerChoices,stores:storeChoices,products,colorStyles},null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`手机采购数据_${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(a.href)});
function exportDetailRows(rows,includeDate=true){const header=includeDate?['日期','购买时间（当前电脑时区）','IMEI','型号','UPC','购买人','金额（美元）','购买门店']:['购买时间（当前电脑时区）','IMEI','型号','UPC','购买人','金额（美元）','购买门店'];return[header,...rows.sort((a,b)=>new Date(a.purchasedAt)-new Date(b.purchasedAt)).map(r=>{const base=[new Date(r.purchasedAt).toLocaleString('zh-CN',{hour12:false}),r.imei||'',r.model,r.upc,r.buyer,Number(r.price),r.store];return includeDate?[dayKey(r.purchasedAt),...base]:base})]}
$('exportAllBtn').addEventListener('click',async()=>{if(!records.length){alert('还没有采购记录可导出。');return}const byDay=new Map();records.forEach(r=>{const day=dayKey(r.purchasedAt),list=byDay.get(day)||[];list.push(r);byDay.set(day,list)});const sheets=[{name:'全部记录',rows:exportDetailRows([...records])}];for(const [day,rows] of [...byDay.entries()].sort(([a],[b])=>a.localeCompare(b))){sheets.push({name:day,rows:exportDetailRows(rows,false)})}try{downloadXlsx(`全部采购记录_${dayKey(new Date())}.xlsx`,await window.phoneLedger.xlsx(sheets));}catch{alert('导出 Excel 失败，请重试。')}});
function safeSheetName(name,used){let base=String(name).replace(/[\\\\/:?*\[\]]/g,'').trim()||'未命名';base=base.slice(0,31);let result=base,n=2;while(used.has(result)){result=`${base.slice(0,28)}_${n++}`.slice(0,31)}used.add(result);return result}
$('exportDayBtn').addEventListener('click',async()=>{const date=$('exportDate').value;if(!date){alert('请先选择要导出的日期。');return}const rows=records.filter(r=>dayKey(r.purchasedAt)===date);if(!rows.length){alert(`${date} 没有采购记录。`);return}const byBuyer=new Map();rows.forEach(r=>{const list=byBuyer.get(r.buyer)||[];list.push(r);byBuyer.set(r.buyer,list)});const overview=[['日期','购买人','采购数量','总金额（美元）','型号统计'],...[...byBuyer.entries()].sort(([a],[b])=>a.localeCompare(b)).map(([buyer,list])=>{const models=new Map();list.forEach(r=>models.set(r.model,(models.get(r.model)||0)+1));return[date,buyer,list.length,list.reduce((s,r)=>s+Number(r.price),0),[...models.entries()].map(([m,n])=>`${m} ×${n}`).join('；')]})];const sheets=[{name:'总汇总',rows:overview}],used=new Set(['总汇总']);for(const [buyer,list] of [...byBuyer.entries()].sort(([a],[b])=>a.localeCompare(b))){const groups=new Map();list.forEach(r=>{const g=groups.get(r.model)||{model:r.model,count:0,total:0,upcs:[],imeis:[]};g.count++;g.total+=Number(r.price);g.upcs.push(r.upc);if(r.imei)g.imeis.push(r.imei);groups.set(r.model,g)});sheets.push({name:safeSheetName(buyer,used),rows:[['购买人：',buyer],['型号','数量','总金额（美元）','UPC','IMEI'],...[...groups.values()].map(g=>[g.model,g.count,g.total,g.upcs.join(' / '),g.imeis.join(' / ')]),[],['购买时间（当前电脑时区）','IMEI','型号','UPC','金额（美元）','购买门店'],...list.sort((a,b)=>new Date(a.purchasedAt)-new Date(b.purchasedAt)).map(r=>[new Date(r.purchasedAt).toLocaleString('zh-CN',{hour12:false}),r.imei||'',r.model,r.upc,Number(r.price),r.store])]})}try{downloadXlsx(`${date}_采购记录.xlsx`,await window.phoneLedger.xlsx(sheets));}catch{alert('导出 Excel 失败，请重试。')}});
$('importInput').addEventListener('change',async e=>{const f=e.target.files[0];if(!f)return;try{const data=JSON.parse(await f.text());if(!Array.isArray(data.records))throw Error();if(!confirm(`将导入 ${data.records.length} 条记录，并替换当前 ${records.length} 条记录。继续吗？`))return;records=data.records;buyerChoices=data.buyers||buyerChoices;storeChoices=data.stores||storeChoices;products=data.products||products;colorStyles=data.colorStyles||colorStyles;await save();render();alert('导入完成。')}catch{alert('无法读取此数据文件，请选择由本软件导出的 JSON 文件。')}finally{e.target.value=''}});
$('settingsBtn').onclick=()=>{$('settingsCard').hidden=false;refreshSettings();renderColorStyles()};$('closeSettings').onclick=()=>{$('settingsCard').hidden=true};
function refreshSettings(){[['buyerChips',buyerChoices],['storeChips',storeChoices]].forEach(([id,choices])=>{$(id).innerHTML='';choices.forEach((value,index)=>{const chip=document.createElement('span');chip.textContent=value+' ';const del=document.createElement('button');del.type='button';del.textContent='×';del.onclick=async()=>{choices.splice(index,1);await save();refreshSettings();updateOptions()};chip.append(del);$(id).append(chip)})})}
async function addChoice(inputId,choices){const value=$(inputId).value.trim();if(!value)return;if(!choices.some(x=>x.toLowerCase()===value.toLowerCase()))choices.push(value);$(inputId).value='';await save();refreshSettings();updateOptions()}
$('addBuyer').onclick=()=>addChoice('newBuyer',buyerChoices);$('addStore').onclick=()=>addChoice('newStore',storeChoices);$('newBuyer').onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();addChoice('newBuyer',buyerChoices)}};$('newStore').onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();addChoice('newStore',storeChoices)}};
$('addProduct').onclick=async()=>{const upc=$('newUpc').value.replace(/\D/g,''),model=$('newProductModel').value.trim(),color=$('newColor').value.trim(),storage=$('newStorage').value.trim();if(!upc||!model){alert('请填写 UPC 和型号。');return}if(products.some(p=>p.upc===upc)){alert('这个 UPC 已存在。');return}products.push({upc,model,color,storage});['newUpc','newProductModel','newColor','newStorage'].forEach(id=>$(id).value='');await save();refreshSettings();updateOptions()};
const baseRefreshSettings=refreshSettings;refreshSettings=function(){baseRefreshSettings();const box=$('productList');box.innerHTML='';products.forEach((p,i)=>{const row=document.createElement('div');row.className='product-row';row.textContent=`${p.upc}｜${p.model}｜${p.color||'-'}｜${p.storage||'-'} `;const b=document.createElement('button');b.type='button';b.textContent='删除';b.onclick=async()=>{products.splice(i,1);await save();refreshSettings();updateOptions()};row.append(b);box.append(row)})};
function renderColorStyles(){const box=$('colorStyleList');box.innerHTML='';colorStyles.forEach((style,i)=>{const row=document.createElement('div');row.className='color-style-row';const tag=document.createElement('span');tag.className='color-tag';tag.textContent=style.name;tag.style.color=style.textColor;tag.style.backgroundColor=style.bgColor;tag.style.borderColor=style.textColor+'55';const del=document.createElement('button');del.type='button';del.textContent='删除';del.onclick=async()=>{colorStyles.splice(i,1);await save();renderColorStyles();render()};row.append(tag,del);box.append(row)})}
$('addColorStyle').onclick=async()=>{const name=$('colorStyleName').value.trim(),textColor=$('colorStyleText').value,bgColor=$('colorStyleBg').value;if(!name){alert('请先填写颜色名称。');return}const old=colorStyles.find(s=>s.name===name);if(old)Object.assign(old,{textColor,bgColor});else colorStyles.push({name,textColor,bgColor});$('colorStyleName').value='';await save();renderColorStyles();render()};
(async()=>{const data=await load();records=Array.isArray(data)?data:data.records||[];buyerChoices=data.buyers||[];storeChoices=data.stores||[];products=data.products||[];colorStyles=data.colorStyles||[];$('exportDate').value=dayKey(new Date());render();refreshSettings();renderColorStyles()})();
